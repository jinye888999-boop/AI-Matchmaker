<view class="card header">
  <view class="title">我的</view>
  <view class="meta">欢迎回来，继续完善个人资料与账号设置</view>
</view>
<view class="menu card" wx:for="{{menus}}" wx:key="path" bindtap="go" data-path="{{item.path}}">{{item.name}}</view>
